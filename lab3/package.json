{
  "name": "lab3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -d dist -b gh-pages -e lab3"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "sass": "^1.77.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "swiper": "^12.0.3"
  }
}
